name: KlipperLH_env
channels:
  - conda-forge

dependencies:
  - python=3.12
  - pip
  - websockets
  - mkdocs-material
  - pydantic

  # Packages not on conda-forge (or better installed via pip)
  - pip:
